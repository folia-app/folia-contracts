// let instance = ScammerController.deployed()
// mints 500 collection (tokenIds 1000001 to 1000500) as paused
// instance.addCollection(500, web3.utils.toWei("0.1", "ether"), true)

// unpauses collection
// instance.updateCollectionPaused(false)

// instance.buy(accounts[0], 1000001, {from: accounts[0], value: web3.utils.toWei("0.1", "ether")})
